@model ServiceWebsite.ViewModels.Common.ImageViewModel
@{
    var htmlFieldPrefix = ViewData.TemplateInfo.HtmlFieldPrefix;
    if (!String.IsNullOrWhiteSpace(htmlFieldPrefix))
    {
        htmlFieldPrefix = htmlFieldPrefix + ".";
    }
}
<li  class="remove-img"  >
    @using (Html.BeginCollectionItem(htmlFieldPrefix+"ImageViewModels"))
    {
        if (!string.IsNullOrWhiteSpace(Model.BlogUniqueTempId))
        {
            <img src="@Url.Content(SystemConstants.BlogTempImageThumbPath + Model.BlogUniqueTempId + "/" + Model.FileName)" width="50" height="50"  />

        }
        else
        {
            <img src="@Url.Content(String.Format(SystemConstants.BlogImageThumbPath) + "/" + Model.FileName)"    width="50" height="50" />

        }
        @Html.HiddenFor(a => a.FileName)
        @Html.HiddenFor(a => a.Alt)
        @Html.HiddenFor(a => a.Title)


    }
    
</li>

<script>

    $(".remove-img").click(function () {

        $(this).remove();
    });
</script>